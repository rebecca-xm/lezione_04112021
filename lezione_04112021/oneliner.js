// element.name.toLowerCase().search(value) <- il problema del one liner è che non può esssere debuggato

// element.name
//     .toLowerCase()
//     .search(value)

//     const str = element.name;
//     const lower = str.toLowerCase();
//     const src = lower.search(value);